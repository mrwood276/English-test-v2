# 05 TASK QUEUE

Statuses: READY, IN_PROGRESS, BLOCKED, COMPLETED, PLANNED, CANCELLED. Priorities: CRITICAL, HIGH, MEDIUM, LOW.
Order follows dependencies. Completed tasks are listed at the end for history (all verifiable from git, migrations, or the design document).

## NEXT RECOMMENDED TASK

**Deployed live (2026-09-22, fifth session):** `question-bank` v3 (with `import_check`/`import`) and the new `exams` function are both live on the v2 project, and the exam SQL is applied (first migration in git: `supabase/migrations/20260922000000_exams_functions.sql`). The import screen now works against the real backend — `import_check` answered live. Remaining for TASK-006: step 4 (owner review of the file formats; templates are ready on the screen) and one real Excel/Google-Sheets file check (ISSUE-013 caveat).

**Done since (2026-09-23, seventh session):** **TASK-010 is built and live-verified** — students join by code and take the test end to end (SQL engine + `session` function + the student page), see the changelog and `docs/sql-sessions.md`.

**Done since (2026-09-24, eighth session):** **TASK-012 (core) is built and live-verified** — essay grading, the per-exam results screen, the attempt report, add time / reopen (BR-11) and retake permissions (BR-02); **ISSUE-017 closed**. Contract in `docs/sql-results.md`.

**Done since (2026-09-23, tenth session — full review pass by Claude/claude.ai, AI Development Reviewer):** ISSUE-020 closed (35 functions were callable by `anon`/`authenticated`, bypassing every Edge Function — fixed live and in git); TASK-013 monitor SQL applied live and verified (F-13 → LIVE-VERIFIED); then **every automated check in the repo was actually re-run, not just read from `.ai/`** — 104 backend tests, 21 import-parser unit tests, and all **9** Playwright suites (teacher, bank, editor, media, import, exams, student, results, monitor) — all green. Code-quality scan (conflict markers, hardcoded secrets, `console.log`/`debugger`, external CDN references, orphaned files, the DEC-021 import duplicate) came back clean on `ai-development`. See `07_CHANGELOG.md` for the full writeup.

**Done since (2026-09-24, eleventh session — Buffy):** **TASK-022 COMPLETE** (a real browser watched `#/monitor` against a real running exam — 20/20 checks) and **TASK-013's last gap closed**: exam-wide add time plus the monitor-payload drift fix (ISSUE-021). See `07_CHANGELOG.md` and `docs/sql-monitor.md`.

**Done since (2026-09-24, twelfth session — Buffy):** the remote was read **before** building (the lesson from the collision): a GitHub Copilot session had already shipped the CSV export and the Classes/Questions tabs, so they were **verified** (backend 106, unit 21, nine suites green at `46dafac`), two real defects in the new Questions tab were **fixed**, and the **Excel export** of mockup 14 was built — a dependency-free ZIP/xlsx writer whose output the repository's own import reader reads back in the unit tests (unit 21 → **31**, `results_e2e` 64 → **79** checks). TASK-012 is now down to the PDF class summary.

**Done since (2026-09-24, thirteenth session — Claude/claude.ai, AI Development Reviewer):** **TASK-008 closed** — all 12 missing migrations (`v2_01`..`v2_12`) pulled from the live project and committed to git verbatim; `supabase/README.md` updated. Found (and documented, not yet fixed — needs a real DB connection) a small reverse discrepancy: three already-committed migrations have no matching row in the live migration-tracking table.

**Done since (2026-09-24, fourteenth session — Codex):** **TASK-014 COMPLETE** — the dashboard now matches mockup 5 (big exam code, live preview, essays/page-exit attention rows, recent pass-rate meters) and the phone shell has a compact one-row menu. `list_exam_activity` gained `passed`/`failed` additively and was applied live; no Edge Function redeploy was needed.

**Done since (2026-09-24, fifteenth session — Codex):** the first dashboard CI run exposed a test-setup error — `dashboard_e2e.py` called `open_exam_row(CODE)` before registering the session exam. The suite now calls `session_exam(CODE)` first and then mirrors it with `open_exam_row(CODE)`. At `2ea6ac0`, Backend run #36 and Frontend run #28 (all ten mocked browser suites, including the dashboard) are green.

**Done since (2026-09-29, sixteenth session — Claude Code):** **TASK-015's first slice — the admin audit-log viewer — is built and fully TESTED in git**: SQL `list_audit_logs` (migration `20260929000000_audit_functions.sql` — the entry below explains that this was in fact applied live in that session's own follow-up), the `audit` Edge Function (admin-only), the `#/audit` screen with filters/paging, the mock-server handler, 8 Deno tests + `audit_e2e.py` (25 checks) + the rolled-back SQL test, and the CI step. Menu assertions went 6→7 in two suites. **The `supabase db query` commands it recorded do not exist** (CLI 2.117.0) — the next entry has the working way.

**Done since (2026-09-25, seventeenth session — Buffy): TASK-015 slice 1 is LIVE-VERIFIED.** The audit migration and the `audit` Edge Function turned out to be **already applied/deployed live** by the previous session's follow-up (recorded as `20260925001719 v2_16_audit_functions`; function deployed 02:39 UTC today) while the docs still said "pending", so this session **verified the live system instead of re-applying it**: the rolled-back `supabase/tests/audit_functions_test.sql` passed live (`AUDIT VIEWER TESTS PASSED`), the admin smoke returned the four real rows with resolved actor names plus correct paging/filters/friendly refusals, row counts were unchanged, and every "not yet applied / deploy pending" note in `.ai/` (F-14, 02, 04, 08, `docs/sql-audit.md`, the migration header) was corrected. Note for the next agent: **`npx supabase db query` does not exist** in CLI 2.117.0 — use the Management API query endpoint or the dashboard SQL editor. The same session found and fixed a **live blocker**: "disable sign-ups" had switched the Email provider off entirely, so all staff sign-in failed with `email_provider_disabled` (ISSUE-007, owner-approved fix: provider back on, sign-ups still refused). It also fixed the **leave-guard defect** behind the two red Frontend CI runs (`f4eb5e7`, `2f5de2c`): a registered guard warned on sight and made the router hold the URL on the current screen, so a reload in that window loaded the previous screen — `exams_e2e.py` was reloading inside that window.

**Done since (2026-09-25, eighteenth session — Buffy): TASK-007 is COMPLETE.** The first real image and audio upload against live Storage was run through the app with the staff test account, saved on a question, reopened and played back — the new `frontend/tests/live_media_check.py` is **32/32**, the protocol matched `@supabase/storage-js` exactly (nothing in `media.js` or the `media` handler needed changing), a forced oversized file was refused and **deleted from Storage again**, and everything the run created was removed and verified gone. **ISSUE-002 is closed.**

**The same session also fixed ISSUE-023** after asking the owner how an exam with attempts should behave (answer: "Close for teachers, permanent delete for the admin only"): migration `20260930000000_exam_delete_with_attempts.sql` + `exams` v4 + the role-aware exams screen + tests, live-verified by `frontend/tests/live_exam_delete_check.py` (**17/17**). See **DEC-027** and `docs/sql-exams.md`.

**TASK-020 is also COMPLETE (2026-09-25, nineteenth session — Buffy, on the owner's instruction).** Another session had left the duplicate scan live and untracked (**ISSUE-024**); its two live statements were committed **verbatim** from `supabase_migrations.schema_migrations.statements` (DEC-028, file names = live version numbers) together with the matching `duplicate_groups` action, and the missing **banner** was built on top: the list shows "N questions look like duplicates of each other" with a **Review** dialog. Live-verified by `frontend/tests/live_duplicates_check.py` (**15/15**, signed in as the staff test account — now possible **without the password**, through a one-time login link). See `docs/sql-duplicates.md`.

**Next recommended: the TASK-015 remainder** (scheduled purge jobs need pg_cron live; backups and notifications need live access or the owner's provider decision, DEC-017). The **PDF class summary** remains the last piece of TASK-012 but needs the owner's decision on what a one-page class summary should hold. **TASK-022, TASK-008, TASK-014, TASK-007, the audit-viewer slice and ISSUE-023 are done.** There is **no open release blocker**; the merge into `main` is the owner's decision — see `08_HANDOFF.md` RELEASE STATUS.
- **TASK-022 — COMPLETE (2026-09-24)**: the live browser run is `frontend/tests/live_browser_check.py` (20/20 against the real project with a real exam being taken). Progress bars render as `1/2`, the countdown as `29m 44s`, the pills as `Saved` / `Left the page` / `Need a look` / done, the board refreshes itself every 15 s, and the exam-wide **Add time to everyone** button moved three running students' clocks in the database while leaving the finished one alone.
- ~~Disable public sign-up (ISSUE-007)~~ — **DONE and verified live 2026-09-25**: `disable_signup: true`, a real sign-up attempt answered `signup_disabled`, and the Email provider is back on so staff can sign in (it had been switched off by mistake).
- ~~Media upload check (TASK-007)~~ — **DONE and live-verified 2026-09-25** (`frontend/tests/live_media_check.py`, 32/32, with the staff test account; everything it created was deleted again).

**Collision note (DEC-021 / ISSUE-015):** `main` carries Codex/GPT-5's own smaller import implementation (`d21f82d`/`1606aed`/`9c293fc`). Per the owner's 2026-09-22 decision, the `ai-development` implementation described here is the one that continues; do not port Codex's variant. The two will be reconciled at the next deliberate merge into `main` (resolve import files in favor of `ai-development`; drop `frontend/tests/import.test.js` and `frontend/assets/js/teacher/import/model.js`).

---

## TASK-006 — Import questions from Excel/CSV and pasted text
- Priority: HIGH. Status: **LIVE (step 3 deployed 2026-09-22; step 4 owner review left)**. Feature: F-08.
- Description: Teachers add many questions at once: upload a `.xlsx`/`.csv` file, or paste text copied from Word. The browser parses and shows a review (ready / needs fixing / duplicate) before anything is saved; saving is all-or-nothing.
- Branch: **`ai-development`** (not on `main` yet — see `08_HANDOFF.md` and DEC-020).
- Done so far: SQL `find_similar_batch`, `import_questions` (live, tested); Edge `parseImportItems`, `parseImportCheckItems`, actions `import_check` and `import`, unit tests (repository only, not deployed). Browser parsers `frontend/assets/js/teacher/import/{csv,zip,xlsx,text,rows,rules}.js` with 21 Deno unit tests in `frontend/tests/unit/import.test.ts` — **`zip.js`/`xlsx.js` are now tested too** (5 tests against `frontend/tests/unit/fixtures/import-sample.xlsx`, a real OOXML package built by `make_xlsx_fixture.py`; ISSUE-013 closed). The import screen `#/questions/import` is built and wired (route, "Import" button, `importCheck`/`import` API calls, defaults panel, review table with Ready/Fix/Duplicate in bank/Duplicate in file/Similar pills, in-file duplicate detection, select-all, leave guard, refused-batch error display); `frontend/tests/mock_server.py` gained `import_check`/`import` handlers; the Playwright suite `frontend/tests/question_import_e2e.py` covers 43 checks including a real `.xlsx` upload through the file picker; template files `frontend/assets/templates/import-template.{xlsx,csv}` (generated by `make_import_template.py`, linked from the screen) and README updates are done.
- Relevant files: `backend/functions/question-bank/handler.ts`, `parse.ts`; `backend/tests/question_bank.test.ts`; `frontend/assets/js/teacher/import/*`; `frontend/assets/js/teacher/screens/questionImport.js`; `router.js`; `screens/questionBank.js`; `api/questionBank.js`; `frontend/assets/js/shared/icons.js` (two icons added); `frontend/assets/css/questions.css` (import block); `frontend/tests/{mock_server.py,question_import_e2e.py}`; `frontend/tests/unit/{import.test.ts,make_xlsx_fixture.py,fixtures/}`; `frontend/assets/templates/*`.
- Dependencies: F-05 editor rules (same validation rules), F-06 reading texts.
- Constraints: parsing happens in the browser with **no new runtime dependencies** (DEC-007); the Deno unit tests use `npm:linkedom` as a dev-only DOM shim for the xlsx reader (test-only, no browser dependency). Only `.xlsx` and `.csv` (not `.xls`). Nothing is saved before the review is confirmed. Batch limit 200. Files (images/audio) cannot be imported.
- Server contract (already implemented):
  - `POST question-bank {action:"import_check", items:[{i, body, options:[text]}]}` → `{results:[{i, matches:[{id, body, similarity, exact, is_archived, used_in_exams}]}]}` (only rows with matches appear).
  - `POST question-bank {action:"import", items:[{row, type, body, difficulty, topic, weight, options:[{body,is_correct}], accepted_answers:[text], essay_guidance, explanation, class_labels:[text], passage?:{title, body?}}]}` → `{created, passages_created, ids}`. A problem returns HTTP 400 with `Row N: <message>` and nothing is saved.
- **PROPOSED formats (the owner has NOT reviewed them; show them clearly in the UI and ask if changes are wanted):**
  - Spreadsheet: first row = header, one question per row. Columns (case/spacing-insensitive, English and Indonesian aliases): `type` (optional: multiple_choice/true_false/short_answer/essay; inferred when blank), `question` (required), `option_a`..`option_f`, `correct` (letter for multiple choice; True/False for true/false; accepted answers separated by `|` for short answer), `guide` (essay), `explanation`, `topic`, `difficulty` (easy/medium/hots), `points`, `class` (labels separated by `,` `;` or `|`), `reading_text` (title) and `reading_text_body` (text; needed only when a text with that title does not exist yet; rows with the same title share one text).
  - Pasted text: questions separated by numbering (`1.` / `1)`) or blank lines; options `A.` / `A)` / `(A)` to F; correct answer by a line `Answer: B` (also `Key:`, `Jawaban:`, `Kunci:`) or by `*` after/before an option; meta lines `Topic:`, `Level:`/`Difficulty:`, `Points:`, `Class:`, `Explanation:`, `Type:`, `Guide:`, `Reading text: <title>`; reading text defined by blocks `[Reading text: Title] ... [/Reading text]`; no options + `Answer: True|False` = true/false; no options + other answer = short answer (accepted answers separated by `|`).
  - Defaults panel (class labels, topic, difficulty, points) applied to rows that leave them blank.
- Review screen: table with import checkbox, row number, question text, type, answer summary, status pills (Ready, Fix, Duplicate in bank, Duplicate in file, Similar) and messages. Rows with errors cannot be imported; exact duplicates are unchecked by default; similar ones are checked with a warning. "Import N questions" then goes back to the list with a toast. Unsaved review triggers the leave guard.
- Steps:
  1. ~~Parsers + row normalization + validation mirroring `save_question` rules, with Deno unit tests (pure JS).~~ **DONE** (21 tests; zip/xlsx included since 2026-09-22).
  2. ~~Import screen `#/questions/import`, route in `router.js`, "Import" button next to "Add question", API calls in `api/questionBank.js`, Playwright test with the mock server (extend `mock_server.py` with `import_check`/`import`).~~ **DONE** (2026-09-22, plus template files and the `question_import_e2e` suite).
  3. ~~Deploy `question-bank` (repository code, includes import actions).~~ **DONE (2026-09-22, fifth session)**: deployed live (v3, `--no-verify-jwt`, via `--use-api` so no Docker needed); `import_check` verified live against the 40-question bank (empty results for a non-matching row), `list` regression-checked (total: 40).
  4. Show the owner the proposed formats (they are now visible in the screen's help text, example button, and template files); README updates are done.
- Acceptance criteria: the three input paths produce the same review model for equivalent content; every parse/validation problem names its row; a batch with any refused row saves nothing; duplicates within the file are detected; hostile HTML in cells is neutralized (server sanitizes, browser displays through `rich.js`); all suites green; template opens in Excel with correct characters (UTF-8).

## TASK-007 — Verify image/audio upload against real Supabase Storage
- Priority: HIGH (was the last release blocker). Status: **COMPLETE and LIVE-VERIFIED (2026-09-25, eighteenth session)**. Feature: F-07. Issue: **ISSUE-002, closed**.
- What was done: `frontend/tests/live_media_check.py` (new, 32 checks) signs the **staff test account** `testguru211l@gmail.com` in, drives the real editor in Chromium against the live project, and checks: the staff gate; the one-time upload link is an absolute Storage URL; the PUTs go straight to Storage (one per file, HTTP 200, `apikey` + `x-upsert: false` + multipart); a 7.6 MB photo is shrunk to an 813 KB WebP; the MP3 registers with its exact byte count and its real 3-second duration; the viewing links render the picture (`naturalWidth > 0`) and play the audio (`readyState 4`, `duration 3`, position advancing); saving attaches both to the question and reopening them plays them again; `media_files`/`question_media`/`storage.objects` all agree; a 4.5 MB JPEG forced into Storage is refused and **deleted from Storage again**; a teacher is refused `purge_unused` (403) and a tokenless call is refused (401); and the run's own rows and objects are deleted and verified gone.
- Result: **32/32**, `ALL LIVE MEDIA CHECKS PASSED`. Live state unchanged afterwards (0 `media_files`, 0 objects, 40 questions). The traces are 9 `audit_logs` rows, left in place on purpose.
- No product change: the protocol reproduced from `@supabase/storage-js` 2.116 was right (absolute signed URL, multipart with `cacheControl` and an empty-name file field, token in the URL so no `Authorization` header). `media.js` and `backend/functions/media/handler.ts` were not touched, so no `06_DECISIONS.md` entry was needed.
- To repeat it: `python frontend/dev-server.py 8123`, then `SUPABASE_TEST_EMAIL=… SUPABASE_TEST_PASSWORD=… [SUPABASE_ACCESS_TOKEN=…] python frontend/tests/live_media_check.py` (needs ffmpeg to build the real MP3; `--keep` leaves the artifacts for inspection).

## TASK-008 — Store database migrations in the repository
- Priority: HIGH. Status: **DONE (2026-09-24)**. Issue: ISSUE-001.
- Description: Export the SQL of `v2_01`..`v2_12` into `supabase/migrations/` (one file per migration, same order/names) so the database can be rebuilt from git.
- Resolution: pulled the exact live SQL from `supabase_migrations.schema_migrations` via Supabase MCP (CLI `supabase link`/`db pull` don't work in a claude.ai chat sandbox — `api.supabase.com` isn't reachable there, and `db pull` needs a DB password anyway) and committed all 12 files verbatim, matching the CLI's naming convention and exact timestamps. `supabase/README.md` updated.
- **Follow-up (small, low priority)**: the reverse gap — `20260922000000_exams_functions.sql`, `20260923000000_session_functions.sql`, `20260924000000_result_functions.sql` have no matching row in `supabase_migrations.schema_migrations` even though they're live — see ISSUE-001's "new discrepancy" note. Needs a real DB connection (CLI + password) to fix properly; not an agent-alone task.

## TASK-009 — Exams: create, edit, code, schedule, selection, templates
- Priority: HIGH. Status: **TEACHER SIDE DONE + LIVE-VERIFIED (2026-09-22)**; the student side (join/exam engine) is TASK-010. Feature: F-09.
- Description: SQL functions + Edge function `exams` + screens per mockup 11: title, duration, passing grade, mandatory exam code (auto-generated, editable, unique among open exams), manual or scheduled availability, late-start policy, selection manual or by filter (pool, draw per student), shuffle, 1 attempt, tab-switch limits 1/3/5 (warn/flag/auto-submit), result visibility, essay pending display, duplicate exam/template, open/close.
- Relevant: tables `exams`, `exam_questions`; `docs/design.md` BR-03..BR-06, BR-17; `_shared/codes.ts`; mockup `docs/mockups/round-2.html` (frame 11).
- Constraints: questions used by an exam are archived, not deleted (already implemented); business rules in SQL functions (DEC-004); audit inside functions.
- Acceptance: teacher can create and open an exam with a code; DB constraints hold; tests at SQL, Deno, and Playwright levels.
- **Follow-up done (2026-09-25)**: the delete rule was repaired after an owner-observed defect — `list_exams` reports `session_count`, a teacher's row for an exam with attempts offers no Delete (the SQL closes it instead), and only the admin can permanently delete it, behind a dialog that names the attempts and the results that go with them (DEC-027, ISSUE-023, `frontend/tests/live_exam_delete_check.py` 17/17).
- Progress (2026-09-22): Edge Function `exams` + 24 Deno tests; list + editor screens (manual/auto selection, schedule, live code check, 1/3/5 tab limits, templates, duplicate); mock-server handlers; `exams_e2e.py` (25 checks). **Live the same day**: the exam SQL was applied to the v2 project (now `supabase/migrations/20260922000000_exams_functions.sql`), the `exams` function deployed (v1, `--no-verify-jwt`), and the whole flow verified with the admin account through the real backend: save draft → get → list → update → open → code-uniqueness refusal → check_code → regenerate_code → duplicate → remove, plus refusal paths (empty manual exam, end-before-start schedule) and the tokenless 401 auth wall. Test rows were cleaned afterwards (0 exams live, 40 questions untouched). Live schema facts (enum columns, position > 0, code CHECK) are recorded in `docs/sql-exams.md`. The student side is TASK-010 (built 2026-09-23 — see below).

## TASK-021 — Export questions to PDF/Word — PLANNED, LOW priority
(Called TASK-010 before 2026-09-23; renamed so that TASK-010 means the student side everywhere, as the owner and `08_HANDOFF.md` already used it.)
Depends on TASK-006 completion. Needs a decision on how to generate PDF/Word without new heavy dependencies (ask the owner).

## TASK-010 — Phase 3: student join and exam engine
- Priority: HIGH. Status: **CORE COMPLETE + LIVE-VERIFIED (2026-09-23)**. Feature: F-11.
- Description: student app (`frontend/index.html`), join with name + class + code, 1 attempt (normalized) or remedial, sessions with a per-session snapshot and answer key kept server-side, server-enforced time with 2-minute tolerance (BR-20), autosave with offline queue (BR-12), submit + automatic grading, add time / reopen (BR-11), rate limiting of joins/code attempts (BR-21, `rate_limit_hit`), `timed_out` cleanup.
- Constraints: answer keys never leave the server (BR-09); sessions keep their own snapshot (BR-10).
- Done (2026-09-23): SQL `supabase/migrations/20260923000000_session_functions.sql` (applied live; contract + schema facts in `docs/sql-sessions.md`), rolled-back SQL test `supabase/tests/session_functions_test.sql`, Edge Function `backend/functions/session/` (deployed v1; signed session token per DEC-022; 21 Deno tests), student page (`frontend/index.html`, `assets/js/student/*`, `assets/css/student.css`), mock-server session handler, `frontend/tests/student_e2e.py` (52 checks), CI updated to run `exams_e2e` + `student_e2e`. Live verification: 27/27 checks through the deployed function with the admin account, then all test rows deleted (0 exams, 0 sessions, 40 questions untouched).
- Add time / reopen (BR-11) and the remedial UI are now **DONE in TASK-012** (2026-09-24, via the `results` function and the attempt report). `expire_sessions()` is written but not scheduled yet (TASK-015).

## TASK-012 — Phase 4: grading, results, statistics, exports
- Priority: HIGH. Status: **CORE COMPLETE + LIVE-VERIFIED (2026-09-24); statistics tabs and exports remain**. Feature: F-12.
- Description: the teacher side of an exam — grade written answers (BR-07), read the results of one exam with merged class names (BR-15), correct a grade by hand (BR-18), act on one attempt (add time, reopen — BR-11; allow/take back a retake — BR-02), plus statistics tabs and exports.
- Done (2026-09-24): SQL `supabase/migrations/20260924000000_result_functions.sql` (applied live; contract + rules in `docs/sql-results.md`), rolled-back SQL test `supabase/tests/result_functions_test.sql` (`RESULT ENGINE TESTS PASSED`), Edge Function `backend/functions/results/` (deployed v1; 11 actions; 18 Deno tests), frontend screens `#/grading`, `#/grading/:examId`, `#/results`, `#/results/:examId`, `#/results/:examId/session/:sessionId` with the Grading/Results menu items and the waiting-essays badge, `frontend/tests/results_e2e.py` (59 browser checks), mock-server `results` handler, CI updated to eight suites. Live verification: **38/38 checks** against the real project through the deployed function (two essays graded, one result `passed`, one `failed`, a manual correction and a reopen/re-grade), then every test row deleted (0 exams, 0 sessions, 0 rate-limit rows, 40 questions). **ISSUE-017 closed.** DEC-023 records the single-writer rule.
- Remaining: the **PDF class summary only**. CSV and Excel are implemented (both from `frontend/assets/js/teacher/export/resultsTable.js`, so the two cannot drift); Questions statistics lazy-load existing staff reports (one per finished attempt) and Classes statistics use the overview payload. No schema migration was needed.

## TASK-013 — Phase 5: anti-cheating events and live monitor
- Priority: HIGH. Status: **CORE COMPLETE + LIVE-VERIFIED (2026-09-23)**. Feature: F-13.
- Done: monitor hub `#/monitor`, per-exam live table `#/monitor/:examId` (mockup 12), session timeline `#/monitor/:examId/session/:sessionId` (events + add time), menu + routes, auto-refresh, `monitor_e2e.py` (23 checks), CI ninth suite, migration `20260925000000_monitor_overview_fields.sql` — **applied live** (Claude reviewer session, via Supabase MCP): `list_exam_results` now returns `answered_count`, `question_count`, `last_heartbeat_at`; verified idempotent-safe re-apply, columns confirmed present in the live schema, `has_function_privilege` confirmed `anon`/`authenticated` still refused / `service_role` still allowed, security advisor re-checked clean afterward. Live state untouched (0 exams).
- Done on top (2026-09-24, eleventh session): **exam-wide add time** (`add_exam_time` + the `results` action + the **Add time to everyone** button on `#/monitor/:examId`, migration `20260927000000_exam_wide_add_time.sql`), and the **payload drift fix** — `list_exam_results` now sends the exam's own `tab_switch_warn_limit` / `tab_switch_flag_limit` / `tab_switch_autosubmit_limit` per row, which the status pill had silently been defaulting (ISSUE-021). The redundant `list_live_sessions` read function was dropped in the same migration (one read path, DEC-024).
- Remaining: nothing in this task. The live verification is TASK-022 (done below) plus `frontend/tests/live_monitor_check.py` (38/38 at the API level).
## TASK-014 — Phase 6: dashboard and UX polish
- Priority: MEDIUM. Status: **COMPLETE and CI-green (2026-09-24, Codex)**. Feature: F-03.
- Done:
  - **Dashboard (mockup 5)**: "Open now" with the exam facts, 52-px access code, Copy code / Change code / Close exam, working count, class pills, a five-student live preview using the existing `liveStatusPill`, and a "Watch everyone" link into the real monitor. "Needs your attention" links pending essays to `#/grading/:examId` and sessions at the flag limit to `#/monitor/:examId`. "Recent exams" links the three latest finished exams to results and shows average plus the passed-percentage meter, or "Waiting for grading". The dashboard refreshes every 30 s and keeps Sign out.
  - **One payload policy**: reads only `exams.list`, `results.activity`, and `results.overview`; `list_exam_activity` was extended additively with `passed`/`failed` (DEC-026). The Edge Function needs no redeploy because it passes the SQL JSON through.
  - **Mobile menu polish**: sticky compact phone header (brand + signed-in person on row 1, all six links in one horizontally scrollable row), closing ISSUE-009.
- Intentionally not built: the mockup's class-merge row. No class-merge UI exists anywhere yet; BR-15 belongs to the results flow, so adding a dead Merge button or a second merge implementation would be worse than leaving it out.
- Verification: migration applied live via Supabase MCP and checked (empty activity list with 0 exams, `pg_proc.proacl` shows service-role-only execute, security advisor unchanged); backend **106** ✓, unit **31** ✓, `deno check` on the changed JS ✓, `git diff --check` ✓. New `frontend/tests/dashboard_e2e.py` covers the open card, attention/recent rows, clipboard, confirm/cancel actions, phone menu and no page errors; the first CI run found its setup error (missing `session_exam(CODE)`), the fix landed in `2ea6ac0`, and Frontend run #28 passed all ten suites. `teacher_e2e.py` covers the empty dashboard.
- Remaining: nothing in TASK-014. Notifications/email remain TASK-015; the PDF class summary remains TASK-012.
## TASK-015 — Phase 7: backups (manual + scheduled), audit log viewer, notifications (dashboard + email), scheduled purge jobs — IN PROGRESS (D-06, D-13).
- Priority: HIGH.
- **Slice 1 — audit-log viewer: LIVE-VERIFIED (2026-09-25)** — migration applied live (tracked as `v2_16_audit_functions`), `audit` Edge Function deployed v1, rolled-back live SQL test passed, admin smoke returned the four real rows. The earlier docs' "live apply pending" was stale; nothing remains here. Admin-only screen `#/audit` (menu item "Audit log", `clock` icon) over `list_audit_logs` (contract: `docs/sql-audit.md`). Files: `supabase/migrations/20260929000000_audit_functions.sql`, `supabase/tests/audit_functions_test.sql`, `backend/functions/audit/{index,handler}.ts`, `backend/tests/audit.test.ts` (8), `frontend/assets/js/teacher/{api/audit.js,screens/auditLog.js}`, `router.js`/`shell.js` (route + ADMIN_NAV), `frontend/tests/{mock_server.py,audit_e2e.py}` (25 checks), `teacher_e2e.py`/`monitor_e2e.py` (menu 6→7), CI step. The Edge action is `list` and the endpoint is the only one restricted to `["admin"]` (design.md 1.2).
- **Remaining in this task:** backups (manual + scheduled), notifications (dashboard + email — the owner must choose a provider, DEC-017), scheduled purge jobs (`expire_sessions`, `purge_orphan_media`, `purge_rate_limits` — need pg_cron on the live project).

## TASK-016 — Create the teacher account and set display names
- Priority: MEDIUM. Status: READY (owner action + one SQL insert into `profiles`).
- Description: The owner creates the teacher in Supabase Auth (Auto Confirm), then a `profiles` row (role `teacher`) is inserted; the admin's `full_name` "Admin" is a placeholder awaiting the real name. A user-management screen is part of TASK-015.

## TASK-017 — Hosting and Auth URLs
- Priority: MEDIUM. Status: PLANNED (needs an owner decision). Choose static hosting for v2; then set the `ALLOWED_ORIGIN` function secret, Supabase Auth Site URL/redirect URLs (password reset), self-host or preload fonts (audit M-7).

## TASK-018 — Run frontend tests in CI
- Priority: LOW. Status: **DONE and green (2026-09-23)**. Issue: ISSUE-006 (closed). **2026-09-22, sixth session:** the first CI failure happened (run #4, Question-editor step) and was fixed at its product root — the duplicate session-expired announcement overwrite (see ISSUE-016); all five suites then verified 3× in a row. **2026-09-23, seventh session:** added the two suites that were missing (`exams_e2e`, `student_e2e`), so CI ran **seven** browser suites — both workflows confirmed green on `b0c56bd`. **2026-09-24, eighth session:** added `results_e2e`, so CI now runs **eight** suites (watch the first run of the new one).
- What was done: new `.github/workflows/frontend-tests.yml` with two jobs — `unit` (Deno: the import parser tests, `--allow-read` for the xlsx fixture) and `browser` (Python 3.12 + Playwright/Chromium + Pillow, generates the media fixtures with the new portable `fixtures_dir.py`, starts `dev-server.py 8123`, then runs all ten suites: teacher, bank, editor, media, import, exams, student, results, monitor, dashboard). At `2ea6ac0`, the whole sequence is green in Actions.
- Note for the first real run: `playwright install --with-deps chromium` needs the runner's apt packages (handled by the flag); if the browser job is slow, cache `~/.cache/ms-playwright` later — not needed to start.

## TASK-019 — v1 cutover
- Priority: LOW (after Phases 3–4). Status: PLANNED. Export v1 results (CSV/Excel) and archive (D-13), switch students to v2, retire v1. Nothing to do in v1 until then (D-15).

## TASK-020 — Duplicate overview banner in the question list
- Priority: LOW. Status: **COMPLETE (2026-09-25, nineteenth session — Buffy)**. Feature: mockup 6. Contract: `docs/sql-duplicates.md`.
- What happened: the session that found **ISSUE-024** (another session's `find_duplicate_groups` + `duplicate_groups` action live with no file in git) deliberately did not commit someone else's work; the owner then asked for it to be brought in and finished. The two live statements were committed **verbatim** (DEC-028) as `supabase/migrations/20260925060607_v2_17_duplicate_overview.sql` and `…_fix_search_path.sql`, the 3-line handler action was matched to the deployed function (verified identical by re-downloading it), and the screen side was written: the banner over `#/questions` (server's own count, one scan per visit, refreshed after archive/restore/delete, hidden — never breaking the list — if the scan fails) and the **Review** dialog (`components/duplicateGroupsDialog.js`: one section per group, "Same text (2 questions)" or "57% alike", every question linked to its editor, closing itself on navigation).
- Tests: backend **120**, `question_bank_e2e.py` **67**, all eleven browser suites green; live `frontend/tests/live_duplicates_check.py` **15/15** on the real 40-question bank (**6 questions: 0 exact groups, 3 similar pairs**), with the SQL function asked directly reporting the same number. Falsified first: with the banner switched off the suite times out waiting for it.
- ISSUE-024 is therefore **CLOSED** (the drift is gone: the live function and the repository now agree, and the public function count is explainable from git).

## TASK-022 — Live-verify the monitor screen against a real running exam
- Priority: HIGH (release blocker). Status: **COMPLETE (2026-09-24, eleventh session — Buffy)**. Feature: F-13.
- Done: `frontend/tests/live_browser_check.py` creates a real exam through the API (non-default tab limits), has four students join through the `session` function (quiet / at the warn limit / at the flag limit / submitted), then drives the **local teacher app against the real project** with a real admin session. **20/20 checks**: the hub lists the running exam with its code; the board shows all four students with real progress bars (`1/2`), a real countdown (`29m 44s`), the exit counts and the four pills (`Saved` / `Left the page` / `Need a look` / done); **Add time to everyone** asks first, toasts `Time added for 3`, and moves the three running students' clocks five minutes in the database while the finished one stays at 0; the board counts down on its own 15-second timer with no manual reload; no console errors. Every test row was deleted afterwards (`cleanup_live_monitor.sql`; live state 0 exams, 0 sessions, 40 questions).
- F-13 is now **FULLY LIVE-VERIFIED** in `03_FEATURES.md`.
- Note for a later session: this is the run the owner may also want to do once by hand — the script is the evidence, the owner's own eyes are the acceptance.

---

## COMPLETED (history)

| ID | Task | Evidence |
|---|---|---|
| TASK-001 | Requirements interview, design document (Draft 4), audit of v1, mockups rounds 1–2 (Phase 0) | `docs/` |
| TASK-002 | Phase 1 foundation: dev Supabase project, schema `v2_01`..`v2_07`, Supabase Auth admin account, shared backend library, `auth-me`, sign-in screen, migration of 40 v1 questions | migrations list, `backend/functions/_shared`, git `1d50148`, `c974be7` |
| TASK-003 | Question bank backend + list/preview screen | migration `v2_08`, `question-bank` function, `questionBank.js` |
| TASK-004 | Question editor + reading texts | `questionEditor.js`, `passageDialog.js`, tests |
| TASK-005 | Images and audio | migrations `v2_09`..`v2_11`, `media` function, `mediaPicker.js`, git `ad9b6d6`; **live-verified 2026-09-25 by TASK-007** (real upload + playback, 32/32) |
